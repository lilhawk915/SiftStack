# Credentials & secrets
.env
input.json
input_foreclosure_daily.json

# State files (generated at runtime)
last_run.json
seen_ids.json
captcha_failed_ids.json
.ancestry_page_loads.json
cookies.json
datasift_cookies.json
dropbox_state.json
photo_state.json
*.tmp
*.bak

# Output (generated data)
output/
logs/
# OnBase PDF cache — one subdir per case, each containing 1-5 PDFs
# from the Montgomery probate docket (captured by onbase_probate_pdf.py).
# MB-scale, ephemeral, regenerated per run; never committed.
onbase_cache/

# DataSift wizard debug screenshots (saved to repo root by
# datasift_core.screenshot — useful for debugging upload-flow issues,
# never meant to be committed).
datasift_*.png
/tmp/datasift_*.png

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Browser profiles (contain login data)
.ancestry_profile/
.datasift_profile/
.playwright/

# Apify local storage
apify_storage/
storage/

# Test data & photos
test_photos/

# Skills distribution ZIPs (large, distributed separately)
Skills for REI/
*.skill
*.plugin

# IDE & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Claude Code / planning
.claude/
# .planning/ is tracked — GSD framework artifacts (PROJECT.md, ROADMAP.md, etc.)
.playwright-mcp/

# Misc
node_modules/
lists-hub/
_brand_tmp/
_tmp_skill/
autoresearch/
.pytest_cache/
